---
type: state_archive
created: 2026-09-09
status: archived
last_edited_by: agent_mondrian
tags: [state_archive, blueprint, polyglot, verbatim]
---

# STATE archive — Blueprint-era sections relocated verbatim at campaign close (2026-09-09)

> Relocated from `STATE.md` when Operation Blueprint completed and the file was rewritten current
> (SO-3/SO-7 — nothing deleted; precedent: `how/state_archive_20260803.md` [Keystone→Beacon] and
> `how/state_archive_20260822.md` [Halftone]). Sections below are **verbatim**, including their
> struck-through corrections and their errors-preserved-at-source — the corrections are the record.
>
> Covers: the Blueprint phase banners **P1 · P2 · P2c · P2b · P3 · P4** (2026-08-24 → 2026-09-08),
> the Halftone completion banner, and Operation Polyglot's cross-vault `openai` GENERATE-lane
> registration (2026-09-07), which was concurrent with Blueprint rather than part of it.
>
> Campaign close records: [`how/campaigns/campaign_canvas_blueprint/campaign_canvas_blueprint.md`](campaigns/campaign_canvas_blueprint/campaign_canvas_blueprint.md)
> §Completion Summary · [AAR rollup](campaigns/campaign_canvas_blueprint/missions/artifacts/blueprint_campaign_aar_rollup.md).

## Banners, verbatim as they stood at close

> **▶ 2026-09-07 — 🔵 SECOND PAID GENERATE LANE REGISTERED: `openai` (Operation Polyglot, cross-vault sitting; lease `session_stanley_20260907_000227_polyglot_openai_backend`).** `backends/openai.py` landed as a **thin binding from day one** to the NEW shared layer `Home.aDNA/what/code/openaiapi/` (sibling of `googleai/`; registry `image.pro`→`gpt-image-2` at $0.03/1K·$0.05/2K·$0.08/4K **probe-verify pending**, lane order `SS_OPENAI_API_KEY`-future → `OPENAI_API_KEY` C07 Keychain-staged; ⛔ no credential read in the backend). Registered in `GENERATE_BACKENDS`; `SUPPORTED_ASPECTS` = the honest OpenAI grid (`1:1/3:2/2:3` — narrower than the package default, so plan can't approve what the API refuses). **Doctrine: `adr_008` Amendment 1 `proposed` (§7.7 ratification owed)** — Gemini STAYS the substrate-wide GENERATE default; `openai` is explicit-select only. First named consumer: the SS OpenAI image lane (operator GO 2026-09-06, $10 cap; SS's fence governs SS-side use). Layer suite 13/13 (`openaiapi/tests/`); **comic_render 154/2** (was 143/2 — 11 new backend tests). ⚠ Operator-owed before first paid render: C07 Keychain value stash + a probe run against the live service (`PRICES_VERIFIED_LIVE` flips only from that). CLAUDE.md §Credential Routing gained the image-lane names sentence.

> **▶ 2026-08-22 — 🟢 OPERATION HALFTONE COMPLETE · four memos intaken + answered · the memo backlog SHIPPED · corpus policy RULED · panel export is a CONTRACT (Mondrian, `session_stanley_20260822_review_intake_halftone_close`).** A three-vault review sweep (Canvas · ComfyUI · fleet) opened the session; the operator approved a 3-session plan (this session · charter the successor campaign · a Vulcan session in ComfyUI.aDNA) and granted **memo GO at plan approval** for every memo named in it. **Intake:** the four inbound memos committed byte-unchanged — Hestia's relocation-complete (57 days late via her outbound seam), Lumière's RM-09, Hestia's crossing-doctrine qualification, Iris's panel-export contract ask. **Rulings:** **(1) `adr_010` — artifact corpus policy** (`proposed`, §7.7 pending): `what/artifacts/` stays gitignored, canonical on-node, **backup-registered** (Home WI-16 scope), no off-node fetch path promised — one ruling that answers Hestia's call #1, Lumière's standing risk, and Iris's question (b). A defect it surfaced: the **visual-DNA schema documents were living on the gitignored pixels shelf** — relocated → `what/docs/visual_dna_schema/` (tracked), broken CanvasForge self-cites repointed, §5.2 gains the SS 2D-consumer note. **(2) Crossing doctrine** — both Hestia qualifications ACCEPTED and applied in `context_canvas_topology_graphs.md` v1.1 (size/density bound + angle qualifier on principle 1; principle-5 hub-extraction question recorded open pending Home P5.4). **(3) `spec_panel_export_contract.md` v1.0** — Iris's three properties (filenames · ordering · resolution floor) promoted convention→contract, zero code changed; ⚠ lexicographic filename sort is explicitly NOT contract (unpadded indices). **Memo wave (13 copies into 9 vault inboxes):** the 4 staged memos DELIVERED (the overdue **Rosetta Imagen migration** — with a dated late-delivery note, the deadline passed 5 days ago, "will break" now reads "is broken" · Argus reject-vocabulary gate · Callisto · Berthier) + 4 new replies DELIVERED (Hestia ack ×2 threads · Iris contract reply · SS NV-A5/loop-close — their July in-vault-panel memo's premise died when H3 rendered, and they now know). **Then the close: H6 re-opened and Operation Halftone COMPLETED.** `CV-COMIC-STYLE-01` **implemented + H3-calibrated** (LOO chi-square to run centroid; consistent max 0.987 / mean 0.408 on the 27 eye-gated panels; synthetic grayscale break 1.482; **threshold 1.20**; 6 tests; calibration record filed). Real-DPI evidence cited (the H3 export report: 4× 2062×3150, 0 warnings). **`canvas_comic` ARCHIVED** per adr_009 → `what/production/_archive/` — with the importer census **corrected 1→4 by measurement (F-H6RE-1)**, all four dispositioned, excised tests preserved verbatim. **F-H6RE-2:** the shelf federation-validation module was validating SS/CC legacy wrappers **their owners retired** — now skip-guarded with a dated record. **Environment restored:** the documented `adna-canvas-std` editable install had drifted out of the runner env — reinstalled; the 2 subprocess independence tests pass again. **Suites at close: canvas_core+presentation 922/5 · comic_render 143/2 · producers 259 · canvas_std 115/10 · cert 11/11 · firewall diff 0.** Campaign records: mission h6 `completed` (second AAR) · master `status: completed` + Completion Summary · AAR rollup filed. *(Halftone-era banners + session sections → `how/state_archive_20260822.md`, verbatim.)*
>
> **▶ 2026-08-24 — 🟢 BLUEPRINT P1 (DOCTRINE): Canvas-owned half SHIPPED · the legacy was misdiagnosed, and measurement caught it (Mondrian, `session_stanley_20260824_blueprint_p1_doctrine`).** P1 opened at the operator's gate; `mission_b1_doctrine` created and closed **complete-with-open-item**. **The finding:** P0's draft — sitting in Rosetta's inbox awaiting a ruling — described the 196 template `.canvas` files as *bare JSON Canvas, no `_reserved`, invisible to tooling*. Re-measuring says the **count was right** (196 real files / 46 live vaults, +74 archived) and the **diagnosis was wrong**: **196/196 carry** `{authority: "view", source_yaml, last_sync, sync_hash}` — already the `view` authority row the draft proposes to invent — written to **`metadata._reserved`** instead of the Standard's canonical **`metadata.frontmatter._reserved`**. Green `[OK]` at `core`; fails `adna_native` on **A-2 alone**. *Present-and-unread, since 2026-02.* **Deliverables:** **erratum v2 → Rosetta** (v1 called the fix "a relocation plus two identity fields" — too glib; A-6 wants a **nested `sync` block with a recomputed 16-hex hash**, so v2 carries the **executed** recipe: scratch-migrated 4/4 → `adna_native [OK]`, degradation D-1/2/3 intact, zero node/edge change) · **`adr_011`** rules the reconciliation (`proposed`, §7.7 pending): the legacy **is** the `view` row, canonical placement is normative, migration **offered not performed** (one `.adna` edit + a release reaches all 46 — the files are byte-identical) · **LIP-0010 assessment**: `authority` is **normative in the doctrine but unvalidated by `canvas_std`** (0 of 21 in-vault `adna_native` canvases carry it; `authority: "veiw"` passes silently) — Option B (optional validated enum, additive, v2.4.0) **recommended and deferred**, gated on Rosetta ratifying the pattern; **no schema change taken** · the campaign's own standing order cited an **invalid CLI value** (`--level adna-native`) — fixed. **Gate evidence: `canvas_std` 115/10 · certification 11/11 · firewall diff 0 · `what/lattices/examples/` untouched.** 🔓 **b1.5 (finalize the pattern with Rosetta) stays OPEN** — the phase does not advance on their silence. ▶ **next gate = P2, HOLD.**
>
> **▶ 2026-09-04 — 🟢 BLUEPRINT P2 (RAIL + DOGFOOD) SHIPPED · Canvas exited all-rights-reserved · three inbound memos intaken and answered (Mondrian, `session_stanley_20260904_blueprint_p2_open_and_the_licensing_act`).** Cold start found three untracked inbound memos and a licensing exposure `STATE` did not predict. **(A) LICENSING — Canvas was public and unlicensed since the 2026-06-22 flip.** `LICENSE` installed at root, **MIT © aDNA Labs** (operator ruling; ADR-024 default), **bytes copied** from `Exchange.aDNA/LICENSE`, `md5 b189a96420df57c630764b57ba7ff2f4` verified at source and destination — never composed. Forge readings straddle the push (pre-push 2026-09-04T16:07Z: 9 public repos, 4 unlicensed, Canvas in the numerator). ⛩ **F-P2-1 — Canvas was licensed in fact, in the place that mattered least:** `canvas_std/LICENSE` + `canvas_context/LICENSE` have been tracked, public and byte-identical MIT since **2026-06-13 — nine days *before* the flip.** The forge's `license=NULL` was *correct* (it detects on the root). So the **reference implementation** was licensed the whole time and the **specification documents** — the only artifact a third party implements *from* — were not. The peer diagnosis was half true, on the worse half. ⇒ `license=NULL` means *the root is bare*, not *the repo is unlicensed*; those diverge wherever a repo ships packages. **(B) PUBLICATION BOUNDARY — `adr_012` (`proposed`, §7.7 pending):** fix-forward, no history rewrite; correspondence + session history + closed artifacts are **records**, untouched; authored content redacted (3 files). ⭐ **F-P2-2 — an external instrument confirms a hypothesis; only an internal one enumerates.** Two peers independently measured **2** occurrences of the forge overlay address over 2 files; an inside census **confirmed their number exactly** *and* found a **second RFC1918 literal of the same class at 8 occurrences over 7 files** neither could see. Treating their file list as the work order would have under-remediated **4×** while reporting completion. **(C) BLUEPRINT P2 — `mission_b2_authoring_rail` completed:** the rail `how/skills/skill_canvas_context_diagram.md` + Canvas's **first two dual-channel canvases** (`context_canvas_surface_legs`, `adr_004_production_code_layout` — the vault shipped **zero** until today) + a producer-side `authority` enum check that is **currently the only enforcement anywhere** (F-B1-2). **Building the pattern broke it twice → erratum E2:** the authority axis mixes *who owns the meaning* with *how it is produced* (our canvases are `dual_channel` **and** machine-generated, so the "never hand-edit" discipline attaches to nothing); and the **conformance floor was unsatisfiable ~~by any canvas for 13 months~~ → by any *knowledge-canvas-profile* canvas with a titled group, for ~1 month** (F-P2-3, **corrected 2026-09-06 → F-P2-6**: the two traps cannot conflict before the later was added, `2026-08-03`; and comic-profile canvases always cleared it. `CV-HIERARCHY-01` requires a heading marker, `CV-LEAD-COST-01` forbids `h1/h2/h3`; `#####` passes both only by *not being a heading*; **`####` alone** passes honestly). `diagram_generator`'s **own example** had failed three traps since Atelier (~2.5 months) at **~14% shown** — the gate is declared mandatory and was not being run. Generator repaired (title slot · content-sized code node · ratio-scaled group padding · title/rank overlap); all three canvases now `--strict` clean. **(D) Drop-box adopted** (`who/coordination/inbox/`, `status: open`) — the `-uall` collapse trap **verified in this tree**, not accepted on report. **Gates: `canvas_std` 115/10 · `diagram_generator` 44 · cross-producer sweep 267 · firewall diff 0 · both dogfood canvases `adna_native [OK]` + visual `[OK]`.** ⚠ **NOT met: the Amendment-1 agent-confirmed render** — attempted twice, abandoned when `screencapture` (whole-screen, desktop app) recorded a third party's private messages; captures deleted, operator's window restored. Canvases are **`visual_gate: pending`**; the machine check is **not** a substitute. ▶ **next gate = P2b (conversion memos #10/#11) or P3, HOLD.**
>
> **▶ 2026-09-07 — 🟢 BLUEPRINT P2c (PRODUCER RE-GATE) SHIPPED · the shelf now passes the gate it asks others to adopt · three findings, one of them a rule I nearly shipped that would have silenced the gate (Mondrian, `session_stanley_20260907_blueprint_p2c_producer_regate`).** Folded into Blueprint as **P2c**, a dated scope amendment, discharging `mission_b2`'s own follow-up (a) — which its AAR had sequenced *ahead of* the P2b conversion memos, because *"offering conversions while our own shelf fails the gate repeats the credibility problem the dogfood just fixed."* **The census re-derived, not re-read:** F-P2-6's 99 findings / 13 HIGH / 0 CRITICAL reproduce exactly — and re-deriving it row by row found ⛩ **F-P2-8: the published file count is 7, not 6.** Small, but it is *the record that established* **state the population on the face of the number** (Hopper's ADR-011 A8 §5), it is instance seven of that class, and it survived the F-P2-7 correction pass two days later ⇒ **stating a population does not verify it; only re-deriving does.** **(A) THE FIX, ONCE — `canvas_core/layout_fit.py` (+64 tests).** 89 of 99 findings were four classes already solved in `diagram_generator`; every producer carried its own `est_text_height(wrap=…, line_h=…)` guess while the traps measured the Obsidian flex box. The new module mostly *delegates* — `fit_text_height` wraps the very `obsidian_required_node_height()` that `CV-TEXT-BOUNDS-01` prints in its own fix hint — so a producer that sizes through it passes **by construction**. All 6 producers retrofitted (`diagram_generator`'s interim mirrored constants retired), `_scaffold` carries it with a worked example, and the skill's gate command gained `--profile`. **(B) THE `deck` PROFILE — and ⭐ F-P2-10: a profile is a RE-AIM, not a relaxation.** Ruled expecting 19→11; measured **13**, because a deck profile must also *admit* the `deck-specific` and presentation traps `knowledge-canvas` suppresses. Two checks the default run could not see — including a **HIGH** (`CV-AUDIENCE-01`, which had **never run against a real deck**). F-P2-6 caught the wrong profile *over*-reporting; it was *under*-reporting on the same file in the same runs. **(C) ⚠ F-P2-13 — the governance rule I wrote first would have made 13 of 14 traps non-gating.** The operator ruled ungraduated traps report-but-don't-gate; the obvious predicate (`graduated: false and accepted == 0`) reads principled, cites real registry fields, and captures `CV-TEXT-BOUNDS-01` — which fired **55 times this session**. It was audited before being trusted; the III `cycles_*` counters are **unmaintained** (every trap but one reads `fired=0`), so `fired == 0` means *no record*, not *never useful*. Narrowed to `fired > 0 and accepted == 0` → 3 traps. ⇒ ***a governance rule that reads as principled is more dangerous than one that reads as arbitrary, because nobody audits it.*** **(D) F-P2-11 — an honest measurement admits true findings, not just removes false ones:** correct heights exposed a **CRITICAL** page overflow that had always been real (section-atomic pagination, the CANVAS-L-002 residual; both example documents split, reason recorded in the YAML), and restoring the 3 missing PNGs turned `file_missing` into `aspect_drift` — *a check that cannot run is not a check that passes*. Also **F-P2-12** (`CV-AUDIENCE-01` scored the enclosing frame as a slide — an outlier by construction, and it inflated μ/σ) and **F-P2-9** (`CV-LEAD-COST-01`'s fix hint advised `**bold**`, which trips `CV-HIERARCHY-01` — *a failing check says something is wrong; a failing fix hint says what to do, and is believed*). **Assets:** `canvas_grammar.png` + `diagnostic_odyssey.png` generated from a committed, deterministic `what/specs/diagrams/build_diagrams.py` (not opaque binaries — Canvas is publicly clonable), refs collapsed to one canonical path. **Gates: 13 authored surfaces clean, each stated with its profile** (11 producer examples/goldens + the 2 dogfood canvases) · `canvas_std` **115/10** · certification **11/11** · `canvas_core` **937/3** · producers **267** · `comic_render` **154/2** *(was 143/2 — the **+11 is Operation Polyglot's**, `eb8939d`, not this session's; a concurrent lease landed the `openai` GENERATE backend mid-work)* · firewall diff **0** · every example **byte-identical on rebuild** · all 10 `adna_native [OK]`. ⚠ **Population stated:** that is *not* every `.canvas` in the vault — 13 conformance fixtures (deliberately malformed), 15 gitignored `what/artifacts/` files (`adr_010`), and 4 `what/lattices/examples/` (untouched by standing order) are **out of scope by design**, named rather than quietly excluded. ⚠ **NOT met:** the Amendment-1 agent-confirmed render, still carried from P2 (no safe window-scoped capture on this node). ▶ **next gate = P2b (now unblocked) or P3, HOLD.**
>
> **▶ 2026-09-07 (second session) — 🟢 BLUEPRINT P2b (CONVERSION OFFERS) SHIPPED + 🔵 THE S-4 GATE RULED AND OPENED · the defect class we went to report turned out to be ours (Mondrian, `session_stanley_20260907_blueprint_p2b_conversion_offers`).** **(A) ARGUS RULED S-4 — `accepted` = the reviewer's verdict (reading b).** An untracked memo was waiting in `who/coordination/` at cold start; intaken byte-unchanged, `ack_required: false`. Their rationale is the load-bearing half: under reading (a) every stored entry is vacuously `accepted: true`, so ADR-003 §3's ≥80% graduation gate would measure *store admission* rather than *operator judgment* — refusals accumulating toward *"this register is working"*, the exact failure our distinct-trap choice was made to avoid. `REJECT_VOCABULARY_CONFIRMED False→True`; reject `accepted True→False`; **pick `accepted` stays `True`, and that asymmetry IS the ruling** (commented at both sites so nobody "harmonises" them back into the bug). Old guard test **inverted, not deleted**; new test pins both halves. ⚠ **The flip ARMS the path and emits nothing** — measured, not assumed: collector re-run returned `{rejects: 0, iii_lines: 0, skipped: 6}`, store md5 unchanged, because the HR pilot recorded 3 approve / 3 skip. First emitter is the P4 board. Argus's store-hash FYI **recorded rather than acted on as described** — Canvas has *no graduation scan to re-pin*, and the object we write (wrapper store, 6 lines, `dca90b37…`) is **a different file** from the canonical that rotated 28→30 (`a28ec2a1…`); both tabled with their populations. Their three consumer-namespace choices blessed as made; ADR-003 §4 clarification queued their side as Noria OQ-N11. **(B) P2b — and the census that rewrote the mission.** ⛩ **F-P2b-1: "md5-differ" is a literal measurement; "diverged" is a class claim** — I wrote the second into the session plan holding only the first, and re-deriving four hours later showed **2 of 5 Operations pairs are byte-different and semantically identical**. Both charter figures were wrong: **Operations 10, not 5** (two copies — the *tracked source is 5/5 clean*, the **gitignored projection** is what fails) and **ScienceStanley 33, not 29, in three classes**. ⛩ **F-P2b-3 — 9 of the "bare SS files" are OURS**, from the `canvas_comic` producer Canvas archived at Halftone (`adr_009`); today's `comic_generator` emits canonical `_reserved`. The charter had counted our own output as a consumer's hygiene problem. ⭐ **F-P2b-2 — 40 of the 41 errors across BOTH vaults are one class**: C-4 missing explicit `toEnd`, the **F-HR-1 Obsidian-re-save signature** (three Operations files lost **100%** of their `toEnd` keys — 8/8→0, 5/5→0, 5/5→0). Canvas diagnosed that mechanism in its own vault on **2026-08-23** and has carried the fix since as *"a `canvas_context` interaction concern"* ⇒ **it was never internal; it is the fleet's most common conformance failure, and nobody noticed because the files still render perfectly.** **Shipped `canvas_core/conform.py`** (+13 tests): `normalize_edges` clears **40 of 41 mechanically** (7 of 8 files FAIL→`extended [OK]`, no judgement calls, no `authority` needed); `unresolved_edges` **reports and never repairs** — it surfaced the one real defect, a **C-3 dangling edge live in a shipped C08 teaching package**, pointing off `expires_at` at a node id that has never existed in that file, found in ~40ms. Defect signature reproduced in **fixtures, not copies** — the projection is gitignored by its owner and Canvas is public (`adr_012`). ⛩ **F-P2b-5 — I nearly shipped `authority: "view"` into two vaults.** It was a trial-run placeholder, it is **wrong** for every one of these hand-authored files, and `canvas_std` accepted it **silently on all eight** (F-B1-2 exactly as predicted). ⇒ ***a validating tool that does not know a key cannot tell you that you used it wrongly; green on an unvalidated key is silence, not evidence.*** So the offers **split at the doctrine line** and only tier 1 shipped. **Memos #10 (Berthier) + #11 (SS) DELIVERED** — both vaults read-only throughout, quiescent re-probed at act time, memos left untracked. Authority-axis evidence **held as an artifact, deliberately NOT dispatched** as a fourth memo to Rosetta (three already sit unanswered). **Gates: `canvas_core` 951/3** (was 937/3) · `canvas_std` **115/10** · certification **11/11** · producers **267** · `comic_render` **154/2** · **firewall diff 0** · **no new canvas authored this session** (a tool was built, not a surface), so no per-canvas visual gate applies. ⚠ **Still NOT met:** the Amendment-1 agent-confirmed render, carried from P2. ▶ **next gate = P3, HOLD.**
>
> **▶ 2026-09-08 — 🟢 BLUEPRINT P3 (FEDERATION RE-PIN WAVE) SHIPPED · the charter itself failed the re-derivation check, and the index had been measuring the wrong axis (Mondrian, `session_stanley_20260908_blueprint_p3_repin_wave`).** Cold start found **an inbound memo in the drop-box** — caught by the `-uall` rule the box's own README makes binding on the session ritual. **(A) BERTHIER'S REPLY, intaken byte-unchanged (`6ef9e2d`).** Every P2b figure **reproduces first-party**. ⛔ **It corrects us:** memo #10 §5 Q1 said the C08 projection *"is not in version control"* — it is gitignored **by the vault** *and* carries **its own `.git`**, pushed to a shared repo with merged PRs. ⇒ *`gitignore` is a statement about one repository's index; I read it as a statement about existence in version control anywhere, and a nested repo makes those come apart.* Struck in `mission_b2b`; **the sent memo is not edited** (correspondence is a record, `adr_012`). ⭐ **The defect class now has a commit, an author and a date** — `f00bf04`, 2026-07-22, *"canvas: layout adjustments (Jake, in Obsidian)"*: **one commit** strips every explicit `toEnd` **and** adds the dangling edge. C-3 disposition **DELETE** (history walk: the target never existed as a node in any committed revision) — *"your tool was right to refuse it."* And a finding against themselves that **rewrote this phase's scope**: their tracked canvases pass **and have no guard** — no canvas check, **no wrapper** — *"the clean tree is clean by low traffic, not by construction."* **(B) FOUR FINDINGS BEFORE A LINE OF WORK**, from opening the directories the charter cites: **F-P3-1** the wrapper population is **15, not 14** (`WGS.aDNA` adopted **2026-08-10**; index updated 2026-08-22 without it) · **F-P3-2** **Oration is recorded as a refusal and is an adoption** — the index carried *"wrapper NONE, 🔴 the G7 enabling condition"* while Kennedy adopted **the same day we asked**, v2.3.0/`extended`; **five weeks reporting a closed gap as open** · **F-P3-3** their reply sat `staged_unsent` in **their** outbox **34 days** · **F-P3-4** the charter's own dependency, *"adopt VisualDNA lockstep-flip mechanics"*, names an artifact **that was never built** (`skill_lockstep_flip` absent; their P4 authored 3 of 8 skills and is still a stub) — **operator-ruled dropped**, replaced by `spec_federation_contract` **§3**, ours since Keystone. **(C) THE UNCOLLECTED REPLY COST US A SESSION.** ⛩ **F-P3-5** — Kennedy's Finding 2 (estimate a file card's height from the target H1 at 2.3em + embed header) **shipped `9224a3f`, 2026-08-03 — the day before they wrote it**, built partly from *their own* absorbed `canvas_fit_check.py`; verified **at the creation commit**. ⭐ **F-P3-6** — their closing note reports the `CV-LEAD-COST-01`→`CV-HIERARCHY-01` guidance-vs-trap conflict on **2026-08-04**. **That is F-P2-9**, which we rediscovered and fixed at P2c on **2026-09-07**. ⇒ ***an uncollected reply is not a neutral backlog item; it is a finding you will pay full price to rediscover.*** ⛩ **F-P3-7** — their Finding 1's premise is **wrong** and its complaint **right**: tested, a `_reserved` block carrying **only** `conformance_level: "extended"` gives `declared=extended level_reached=extended [OK]` (the key is read at every level; A-checks run only when declared **is** `adna_native`) — their map could have self-declared on 08-04. But **nothing said so**, the absent-key default is `core`, and they **reversed a correct ruling** on that reading. ⇒ **`spec_federation_contract` §2.1a** written (documentation only, **firewall diff 0**) + the §2.1 enum corrected `extended|adna_native` → **`core|extended|adna_native`**. Sequencing vindicated: the wave was about to ask 15 consumers to fill in that very field. **(D) THE CENSUS — both populations.** 18 wrappers/15 vaults · **200 template files/47 vaults partitioned out and named** (the `adr_011` set; P1's 196/46 grew, as every fork inherits four) · **151 authored canvases/19 vaults**. ⭐ **F-P3-8 — the field the wave exists to re-pin has SIX spellings** (`version` · `standard_version` · `substrate_pin` · `builder_version` · `pin` · `pinned_version`, plus Videos correctly indirecting to `MANIFEST` — *better practice than our own §2.1*). ⇒ **no refresh can be mechanised**, so each one needed a human to read fifteen documents, **and a human who skips one leaves no trace** — which is exactly how WGS went unlisted. ⛩ **F-P3-9** — Obsidian keeps `canvasforge/` citing *"the `ZenZachary.aDNA/canvasforge/` sibling precedent"*; **that directory no longer exists**. 11 of 62 path refs are dead, incl. `~/aDNA/node.aDNA` (retired 2026-06-11) in **both** Astro's and WebForge's wrappers, same edit date ⇒ **one ancestor's error, not two careless vaults**. ⚠ The `lattice-labs` pair: **the shim resolves and the file is gone**, so a shim-health check reports green over a dead ref. **(E) ⭐ THE INDEX WAS MEASURING THE WRONG AXIS.** Membership meant *"holds a wrapper"* — so **10 vaults / 49 authored canvases with no seam at all** were invisible **by construction**, and a peer had to tell us. New **§1b** enumerates them; explicitly **not** a to-do list of ten memos (five are already clean; *a wrapper is a commitment to gates, not a badge*). **(F) CONFORMANCE, 106 peer canvases / 18 vaults:** 63 OK · 43 FAIL · **1664 errors**. **One outlier vault (LAVentureGraph) holds 1175 of them**, 1161 in two files — float coordinates, a different class. **Excluding it: 464 of 489 = 95% C-4** ⇒ F-HR-1 generalises and P2b's *"40 of 41"* was no coincidence — **but the unqualified aggregate reports C-4 as a minority at 28% and buries it** (F-P2-12's trap at fleet scale). ⛩ **`C-3` is a bucket**: 20 of its 21 are invalid `"center"` side values in one file; **exactly one** genuinely dangling edge exists fleet-wide — the Operations one, already ruled. *I nearly published "21 dangling edges, a 21× increase" — literally derived from our own validator and false as a class claim.* **(G) F-HR-1 CLOSED** where the damage was made durable: the collector read the re-saved doc, folded verdicts in and **wrote it back**. Now normalizes **between the read and the fold** (`canvas_core` 951→**958/3**, +7). Idempotency **preserved** — persisted only when the collector was already writing; an un-conformed surface with nothing to collect is **reported, not silently rewritten**, per **Berthier's** ruling that a hand-maintained canvas wants the pass **before publish**. `unresolved_edges` still **reports and never repairs**, pinned by a test asserting the dangling edge *survives*. Defect reproduced **in fixtures, never in copies** (`adr_012`); a test asserts the **premise** first. **(H) 8 OF 9 MEMOS DELIVERED**, md5-verified, left untracked, quiescence **re-probed at act time**: Kennedy · Seshat (`ack_required` — the rename ruling) · Pygmalion (×2 vaults) · Astro · Vitruvius · Janus · Persephone · Cartographer (**1175 → 2** by rounding, and the **2 survivors are duplicate node ids — one the empty string — in an entity graph**: F-P2-11 again, an honest measurement admits true findings). ⛔ **#13 → Rosetta STAGED** — live lease, no drop-box, the same condition that refused E2 for two days. ⚠ **Two index rows briefly claimed delivery before the act and were corrected before commit** — the exact failure this phase exists to fix, committed by the agent writing the correction. **Gates: firewall diff 0 · `canvas_std` 115/10 · certification 11/11 · `canvas_core` 958/3 · producers 267 · `comic_render` 154/2 · zero peer writes · all 11 published census figures re-derived at close and reproduce exactly.** ⚠ **NOT met:** the Amendment-1 render, carried a **fourth** time. ▶ **next gate = P4 (ComfyUI canvas seam), HOLD + spend gate.**
>
> **▶ 2026-09-08 (second session) — 🟢 BLUEPRINT P4 (COMFYUI SEAM, BUILD HALF) SHIPPED · the gate we had called impossible for a month was blocked by a sentence, not by a machine (Mondrian, `session_stanley_20260908_blueprint_p4_comfyui_seam`).** Cold start clean: no lease, empty drop-box, `git status -uall` **zero**. **(A) ⛩ F-P4-1 — the Amendment-1 render was carried unmet FOUR times on a claim that was false when written.** STATE said *"needs a window-scoped capture; whole-screen `screencapture` is ruled out — it captured a third party's private messages."* Both clauses true; the conclusion **false**. `Home.aDNA/what/code/window_helpers.py:capture_window()` has taken a **single window by id** (`screencapture -l`, title-pinned, no focus steal) since Prytaneion M1.3 and *cannot* capture a screen — and its own docstring records operator decision **D-A (2026-06-02): eventual home = CanvasForge.aDNA**, which merged into this vault at pt09. The instrument was assigned here fifteen months ago and the assignment was never executed. ⇒ ***a constraint on a method is not a constraint on the capability***, and nobody re-read the sentence for a month. Ported → `canvas_core/visual_capture.py`, credited at source, Home's copy untouched; the safety property is pinned by an **AST-walking test** (exactly one `screencapture` call site, always `-l`) because discipline is what failed. Two hardenings over the source: no cross-vault fallback in vault-id resolution (a fallback id navigates *someone else's vault*), and validated Swift interpolation. **(B) THE GATE MET, AND IT PAID FOR ITSELF TWICE IN ITS FIRST HOUR.** ⭐ **F-P4-2 — the sight gate was defeated by level-of-detail culling, not resolution**: Obsidian **does not draw node text** below a zoom threshold, so the first capture was window-scoped, correctly framed, pin-sharp at **3128×1896** — and contained no readable text anywhere. A crop and a native re-capture proved it before the mechanism was obvious; **no resolution recovers what was never rendered**. Fixed with a zoom step: Home recorded Shift+1 as *"does not fire headlessly"* and cropped around it, but re-measured here **it fires reliably given an `activate` first**, and `obsidian://open` already focuses the leaf — **no click needed** (verified on a second canvas deliberately without one). Also: their `sips -Z 1280` discards ~2.4× of a Retina capture. ⚠ **F-P4-3 — sight caught what both machine checks passed**: *both* dogfood canvases render their **mermaid channel as raw source** behind an un-actioned per-vault trust prompt (Obsidian 1.14). Not a canvas defect; a first-open consumer condition for the whole dual-channel pattern. **Not actioned — a trust grant is the operator's, not an agent's.** **(C) ⛩ F-P4-4 — and the sight gate produces false positives too.** A clipped sidecar embed was written up as a P2c-class guessed-constant defect and "fixed" by deriving the height from `fit_text_height` (1580px vs the inherited 520). The next render refuted it three ways: embeds **scroll** (nothing was unreachable), `fit_text_height` measures **raw markdown** not rendered Meta Bind widgets, and the taller node grew the board past the LOD threshold so **every caption reverted to placeholder bars**. A second attempt at 900 failed identically. Reverted to 520, re-rendered, confirmed. ⇒ ***what catches a false positive is the same discipline that catches a true one: render again after the fix and compare.*** **(D) THE SEAM ITSELF — venue-independent, exactly as spec §4.2 sequences it.** `run_manifest.py` (§3; provenance floor enforced, **absence never fabricated**, unknown keys ignored-and-reported, a variant included iff its image is on disk) · `variant_board.py` (§1.1; **one `choice` per slot** = Vulcan's SO-5 gate with a substrate — the HR pilot's **second consumer** and the first surface that can emit through the S-4 gate) · `tuning_surface.py` (§1.2; `input` knobs + **one** `re_render` action producing a **D1 request record**, with a test that fails if `requests`/`httpx`/`socket`/`subprocess` is ever imported — v0 ships no dispatcher and now cannot acquire one by accident). **Five fixtures now ARE the contract** ComfyUI's emitter must satisfy. ⛔ **The plan's "no new collector needed" was wrong** — the collector is variant-keyed on `verdict`, the board is slot-keyed on `pick`; `selection_sidecar` support was the honest cost, and a reject keyed on the *slot* would have appended cleanly to the canvas and reached **no sink at all** (`fold_variant_responses` gathers by the `<variant_id>.` prefix). Two more defects found: the inherited `_node_size` returns a node **larger than its fit box** whenever no integer multiple of the reduced ratio fits — unreachable at ~1024px, immediate for a 2062×3150 print panel; and manifest-relative paths in file nodes made the file-resolution traps **skip** while printing `0 findings [OK]` (F-P2-11 again). **Gates: firewall diff 0 · `canvas_std` 115/10 · certification 11/11 · `canvas_core` 958→**1035**/3 (+77 = exactly this session's four test files) · producers 267 · `comic_render` 154/2 · both new surfaces `adna_native [OK]` + 11 traps/0 findings **with `--vault-root`** + **agent-confirmed sight**.** **2 memos delivered** md5-verified, left untracked (Hestia via her **drop-box** under a live lease, which its README makes explicitly safe; Vulcan direct, quiescent re-probed at act time). ⛔ **H4 live chain did NOT run — spend deliberately not requested**: their emitter does not exist, and one hand-written manifest over real pixels proves less than the fixtures already do. ▶ **next gate = P5 (close), HOLD.**
