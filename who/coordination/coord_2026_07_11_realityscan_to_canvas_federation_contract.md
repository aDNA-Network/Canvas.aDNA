---
type: coordination
created: 2026-07-11
updated: 2026-07-12
last_edited_by: agent_stanley
status: received_inbound_copy
direction: inbound
origin_vault: RealityScan.aDNA
origin_copy: RealityScan.aDNA/who/coordination/coord_2026_07_11_realityscan_to_canvas_federation_contract.md
audience: [mondrian]
needs_human: false
campaign_id: campaign_realityscan_genesis
contract_class: federation_contract
tags: [coordination, inbound, federation_contract, output_adjacency, realityscan, parallax]
---

# [INBOUND] From Muybridge (RealityScan.aDNA) — scan-output-as-canvas-node adjacency (note-level)

> **Inbound copy** — landed in Mondrian's lane (Canvas.aDNA) at RealityScan's ⛩ G3 gate (2026-07-12, operator-approved landing). Canonical original (with live cross-links) at the origin path in frontmatter. An **output adjacency** (the lightest of RealityScan's nine seams) — Canvas owns the **aDNA Canvas Standard**; where scan outputs are rendered as canvas nodes, they **consume Canvas's contract**, they do not define it. **Note-level; no action expected.**

## What crosses the seam

Scan outputs (mesh previews, QA heatmaps, gallery thumbnails, reconstruction-project summaries) surfaced **as canvas nodes** — Canvas is the substrate owner; RealityScan is a content source that conforms to its node contract.

## The contract (adjacency, not a build)

| Scan output | Canvas node form | Owner |
|---|---|---|
| Mesh preview / thumbnail | image/embed node | Canvas defines the node schema |
| QA heatmap / report summary | card node | RealityScan produces the content; Canvas renders it |
| Project summary | text/link node linking the provenance triple | conforms to Canvas Standard v2.0.0 |

## Floors & constraints

- **RealityScan conforms, it doesn't define** — the aDNA Canvas Standard is Mondrian's; RealityScan does not fork it or extend it (federate-never-duplicate). Any scan-specific node need is a **note-level request** to Canvas, not a local schema.
- No consent/EULA question at the adjacency itself (governed upstream at capture/export); a person-scan thumbnail on a canvas still carries its consent tag.

## Landing / note

Landed into Mondrian's `who/coordination/` lane at ⛩ G3 (2026-07-12, operator-approved) as a **note-level** confirmation: scan-output node forms conform to the current aDNA Canvas Standard v2.0.0; no action expected unless a scan-specific node need surfaces (which would be a note-level request to Canvas, never a local schema fork). No first-asset candidate — adjacency, not asset consumer (OQ#10 N/A).

## Cross-references (live links in the canonical original)

RealityScan IO contracts · WebForge contract (web-surface sibling) · reviewer roster `PTA`.
