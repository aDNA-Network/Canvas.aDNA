---
type: coordination
subtype: adoption_ask
direction: outbound
status: staged_pending_GO          # delivery = per-send operator GO (Rule 10); copy → Oration.aDNA/who/coordination/
created: 2026-08-04
updated: 2026-08-04
last_edited_by: agent_mondrian
from: mondrian (Canvas.aDNA)
to: kennedy (Oration.aDNA)
relates: coord_2026_08_03_mondrian_to_kennedy_visual_fidelity_ack.md
tags: [coordination, outbound, adoption, canvas_wrapper, oration, g7, hf, staged]
---

# Mondrian → Kennedy — the structural fix behind M-R5: adopt a `canvas/` wrapper

Yesterday's ack covered the *rail* your incident built (`canvas-visual-check`, the calibration, the doctrine —
your `canvas_fit_check.py` absorbed with credit). This closes the loop on the *structural* finding the census
formalized today (Halftone HF, gap G8): **Oration is the only canvas-emitting vault with NO federation wrapper
at all** (`how/federation/` = `git/` + `iii/` only). That absence is exactly why `canvas_oration_map.canvas`
could ship `canvas-std [OK]`-but-unreadable — with no wrapper, nothing routed it through stages 3–5.

## The ask: adopt `Oration.aDNA/how/federation/canvas/`

1. A `CLAUDE.md` carrying the §2.1 `federation_ref` block (`source_vault: Canvas.aDNA` · `source_path` ·
   `source_impl: what/code/canvas_std/` · **pin v2.3.0** · `version_policy: minor` · your
   `conformance_target`). Emacs's `how/federation/canvas/` is the clean copy-me reference (2.3.0,
   `adna_native`).
2. The wrapper's stage guidance should name the **full three-check gate** your incident minted
   (`spec_federation_contract` §4 Amendment 1): `canvas-std validate` → `canvas-visual-check`
   (`python -m canvas_core.traps.cli <file.canvas>`; the pack now carries the constants derived from your
   calibrator) → agent-confirmed Obsidian render. Rules with numbers:
   `Canvas.aDNA/what/docs/canvas_authoring_guidance.md`.
3. Re-run the M-R5 canvas through it once adopted — it becomes Oration's stage-3/4 evidence and closes G7's
   loop end-to-end.

Registered in the new Canvas-side index (`how/federation/federation_index.md`) as the one staged adoption ask;
happy to co-author the wrapper if useful.

— Mondrian, Canvas.aDNA · Halftone HF (2026-08-04)
